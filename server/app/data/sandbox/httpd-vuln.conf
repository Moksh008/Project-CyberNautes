ServerRoot "/usr/local/apache2"
Listen 80
LoadModule mpm_event_module modules/mod_mpm_event.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule alias_module modules/mod_alias.so
LoadModule cgid_module modules/mod_cgid.so
LoadModule dir_module modules/mod_dir.so
LoadModule mime_module modules/mod_mime.so
LoadModule unixd_module modules/mod_unixd.so
LoadModule log_config_module modules/mod_log_config.so

User daemon
Group daemon

ServerName localhost

ErrorLog /proc/self/fd/2
LogLevel warn

DocumentRoot "/usr/local/apache2/htdocs"

# Vulnerable configuration for CVE-2021-41773:
# Granting access to the whole filesystem root is the documented misconfiguration
# that makes the Apache 2.4.49 path-traversal exploitable.
<Directory />
    AllowOverride none
    Require all granted
</Directory>

<Directory "/usr/local/apache2/htdocs">
    Options Indexes FollowSymLinks
    AllowOverride None
    Require all granted
</Directory>

# Aliased directories are the path prefix the CVE-2021-41773 traversal escapes from.
Alias /icons/ "/usr/local/apache2/icons/"
ScriptAlias /cgi-bin/ "/usr/local/apache2/cgi-bin/"

<Directory "/usr/local/apache2/icons">
    Require all granted
</Directory>

<Directory "/usr/local/apache2/cgi-bin">
    Require all granted
</Directory>

DirectoryIndex index.html

TypesConfig conf/mime.types
